{{> "public_head"}}
</head>

<body>
    <div class="wrapper {{post_class}}">
        {{> "header"}}
        <main class="container">
            {{{body}}}
            {{#is "home, paged, tag, author"}}
            {{pagination}}
            {{/is}}
        </main>
        {{> "footer"}}
{{> "public_foot"}}
</body>

</html>